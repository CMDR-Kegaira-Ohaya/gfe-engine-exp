# Product workbench note

This is the seeded product-local note file for the first repo-aware save path.

Purpose:
- give the product area one safe writable target under `product/`
- exercise the guarded save pipeline before any controlled case mutation flows are added

Current expectation:
- GUI can edit this note
- if a repo connector is attached, the product-local save command can write here
- if no repo connector is attached, the draft can still be preserved locally only
