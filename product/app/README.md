# product/app

App-level logic belongs here.

Use this area for:
- case loading
- normalized bundle/state wiring
- application shell logic
- controllers, stores, and shared runtime modules

Keep this layer separate from:
- pure GUI rendering in `product/gui/`
- static assets in `product/assets/`
