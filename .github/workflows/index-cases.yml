name: Index Cases (Deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: echo "This workflow is deprecated. Use .github/workflows/index-case-artifacts.yml for the canonical case-folder catalog."
